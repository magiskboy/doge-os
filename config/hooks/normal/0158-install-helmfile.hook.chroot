#!/bin/sh
# Install helmfile from GitHub release (not in Debian apt).
# Use the tarball binary into /usr/local/bin — same pattern as k9s/uv/fnm.
set -e

HELMFILE_VER=1.7.0

TMP=$(mktemp -d)
cleanup() { rm -rf "$TMP"; }
trap cleanup EXIT
cd "$TMP"

curl -fsSL -o helmfile.tar.gz \
	"https://github.com/helmfile/helmfile/releases/download/v${HELMFILE_VER}/helmfile_${HELMFILE_VER}_linux_amd64.tar.gz"

tar -xzf helmfile.tar.gz
install -m 755 helmfile /usr/local/bin/helmfile
